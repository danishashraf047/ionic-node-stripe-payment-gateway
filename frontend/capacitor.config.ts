import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-node-stripe-payment-gateway-frontend',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
